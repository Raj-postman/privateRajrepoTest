# Auto-Generated: 21/10/2022 19:35:17
openapi: 3.0.0
x-jh:
  publishing:
    type: Integration                                 # [Integration], Product 
    stage: dev                                        # [dev], rc, prod
    isPublic: False                                   # [False], True ... Public Production APIs will also be published to jackhenry.dev
    group: EI&S                                       # Group name or High Order Product Name [Common] ... examples: Origin, EI&S, CPS, Fraud,
    name: Identity Management - Enterprise            # Friendly name used in navigation. 25 chars or less please. [info.title]
    docURL: none                                      # Fully formed URL to a place where documentation exists. [none]
info:
  version: v1
  title: Identity Management Services
  description: Identity Management Services API
  contact:
    name: Duane Kelley
    email: DLKelley@jackhenry.com
security:
  - BearerAuth: []
paths:
  '/v2/jh-identitymanagement/adminconfigurations':
    post:
      tags:
        - AdminConfigurations API
      description: AdminConfigurations `Create`
      operationId: AdminConfigurations-create
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstitutionUniversalId'
        # - $ref: '#/components/parameters/pProductId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      requestBody:
        $ref: '#/components/requestBodies/AdminConfigurationsCreateRequest'
      responses:
        # '200':
        #   description: 'OK<hr>
        #     The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        #   content:
        #     application/json:
        #       schema:
        #         $ref: '#/components/schemas/ConfigurationsCreateResponse'
        '201':
          description: 'Created<hr>
            The HTTP 201 Created success status response code indicates that the request has succeeded and has led to the creation of a resource.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminConfigurationsCreateResponse'                
        '204':
          description: 'Created, No Content<hr>
            There is no content to send for this request, but the headers may be useful.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    get:
      tags:
        - AdminConfigurations API
      description: AdminConfigurations `List`
        <br><hr>Filtering - Elements supported by this API to filter results.
        <br>Filter Enumeration=["AdminConfigurationName"]
        <br>Filter Operators Supported=["eq - equal"]
      operationId: AdminConfigurations-list
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstitutionUniversalId'
        # - $ref: '#/components/parameters/pProductId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        - $ref: '#/components/parameters/pOffset'
        - $ref: '#/components/parameters/pCount'
        - $ref: '#/components/parameters/pSort'
        - $ref: '#/components/parameters/pFilter'
        # - $ref: '#/components/parameters/pFields'
        - $ref: '#/components/parameters/pETags'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminConfigurationsListResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/jh-identitymanagement/adminconfigurations/{AdminConfigurationId}':
    get:
      tags:
        - AdminConfigurations API
      description: AdminConfigurations `Inquiry`
      operationId: AdminConfigurations-inquire
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstitutionUniversalId'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pAdminConfigurationId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        # - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pFields'
        # - $ref: '#/components/parameters/pRestrictions'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
            ETag:
              $ref: '#/components/headers/pETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminConfigurationsInquireResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'                
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    put:
      tags:
        - AdminConfigurations API
      description: AdminConfigurations `Replace`
      operationId: AdminConfigurations-replace
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstitutionUniversalId'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pAdminConfigurationId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pIf-Match'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        # - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      requestBody:
        $ref: '#/components/requestBodies/AdminConfigurationsReplaceRequest'
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminConfigurationsReplaceResponse'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '412':
          description: 'Precondition Failed<hr>
            Resources has been updated since last time it was retrieved.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    delete:
      tags:
        - AdminConfigurations API
      description: AdminConfigurations `Delete`
      operationId: AdminConfigurations-delete
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstitutionUniversalId'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pAdminConfigurationId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pIf-Match'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        # - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminConfigurationsDeleteResponse'
        '204':
          description: 'No Content<hr>
            There is no content to send for this request, but the headers may be useful.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '412':
          description: 'Precondition Failed<hr>
            Resources has been updated since last time it was retrieved.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/{InstitutionUniversalId}/jh-identitymanagement/users':
    get:
      tags:
        - Users API
      description: Users `List`
      operationId: users-list
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        - $ref: '#/components/parameters/pInstitutionUniversalId'
        # - $ref: '#/components/parameters/pProductId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        - $ref: '#/components/parameters/pOffset'
        - $ref: '#/components/parameters/pCount'
        - $ref: '#/components/parameters/pSort'
        - $ref: '#/components/parameters/pFilter'
        - $ref: '#/components/parameters/pFields'
        # - $ref: '#/components/parameters/pETags'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsersListResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/{InstitutionUniversalId}/jh-identitymanagement/users/{UserUPN}':
    get:
      tags:
        - Users API
      description: Users `Inquiry`
      operationId: users-inquire
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        - $ref: '#/components/parameters/pInstitutionUniversalId'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pUserUPN'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        - $ref: '#/components/parameters/pFields'
        # - $ref: '#/components/parameters/pRestrictions'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
            ETag:
              $ref: '#/components/headers/pETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsersInquireResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'                
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/{InstitutionUniversalId}/jh-identitymanagement/users-search':
    post:
      tags:
        - Users API 
      description: Users `Search`
      operationId: users-search
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        - $ref: '#/components/parameters/pInstitutionUniversalId'
        # - $ref: '#/components/parameters/pProductId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        - $ref: '#/components/parameters/pOffset'
        - $ref: '#/components/parameters/pCount'
        # - $ref: '#/components/parameters/pLanguage'
        - $ref: '#/components/parameters/pSort'
        - $ref: '#/components/parameters/pFields'
        # -- Custom Parameters --
      requestBody:
        $ref: '#/components/requestBodies/UsersSearchRequest'
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsersSearchResponse'
        # '201':
        #   description: 'Created<hr>
        #     The HTTP 201 Created success status response code indicates that the request has succeeded and has led to the creation of a resource.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        #   content:
        #     application/json:
        #       schema:
        #         $ref: '#/components/schemas/UsersSearchResponse'         
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/{InstitutionUniversalId}/jh-identitymanagement/users/{UserUPN}/claims':
    get:
      tags:
        - Users Claims API
      description: Users Claims `List`
      operationId: users-claims-list
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        - $ref: '#/components/parameters/pInstitutionUniversalId'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pUserUPN'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        - $ref: '#/components/parameters/pOffset'
        - $ref: '#/components/parameters/pCount'
        - $ref: '#/components/parameters/pSort'
        - $ref: '#/components/parameters/pFilter'
        - $ref: '#/components/parameters/pFields'
        # - $ref: '#/components/parameters/pETags'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsersClaimsListResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/{InstitutionUniversalId}/jh-identitymanagement/users/{UserUPN}/groups':
    get:
      tags:
        - Users Groups API
      description: Users Groups `List`
      operationId: users-groups-list
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        - $ref: '#/components/parameters/pInstitutionUniversalId'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pUserUPN'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        - $ref: '#/components/parameters/pOffset'
        - $ref: '#/components/parameters/pCount'
        - $ref: '#/components/parameters/pSort'
        - $ref: '#/components/parameters/pFilter'
        - $ref: '#/components/parameters/pFields'
        # - $ref: '#/components/parameters/pETags'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsersGroupsListResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/{InstitutionUniversalId}/jh-identitymanagement/users/{UserUPN}/groups/{GroupDistinguishedName}':
    get:
      tags:
        - Users Groups API
      description: Users Groups `Inquiry`
      operationId: users-groups-inquire
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        - $ref: '#/components/parameters/pInstitutionUniversalId'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pUserUPN'
        - $ref: '#/components/parameters/pGroupDistinguishedName'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        - $ref: '#/components/parameters/pFields'
        # - $ref: '#/components/parameters/pRestrictions'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
            ETag:
              $ref: '#/components/headers/pETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsersGroupsInquireResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'                
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/{InstitutionUniversalId}/jh-identitymanagement/groups':
    get:
      tags:
        - Groups API
      description: Groups `List`
      operationId: groups-list
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        - $ref: '#/components/parameters/pInstitutionUniversalId'
        # - $ref: '#/components/parameters/pProductId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        - $ref: '#/components/parameters/pOffset'
        - $ref: '#/components/parameters/pCount'
        - $ref: '#/components/parameters/pSort'
        - $ref: '#/components/parameters/pFilter'
        - $ref: '#/components/parameters/pFields'
        # - $ref: '#/components/parameters/pETags'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GroupsListResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/{InstitutionUniversalId}/jh-identitymanagement/groups/{GroupDistinguishedName}':
    get:
      tags:
        - Groups API
      description: Groups `Inquiry`
      operationId: groups-inquire
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        - $ref: '#/components/parameters/pInstitutionUniversalId'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pGroupDistinguishedName'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        - $ref: '#/components/parameters/pFields'
        # - $ref: '#/components/parameters/pRestrictions'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
            ETag:
              $ref: '#/components/headers/pETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GroupsInquireResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'                
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/jh-identitymanagement/tenants':
    post:
      tags:
        - Tenants API
      description: Tenants `Create`
      operationId: tenants-create
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      requestBody:
        $ref: '#/components/requestBodies/TenantsCreateRequest'
      responses:
        # '200':
        #   description: 'OK<hr>
        #     The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        #   content:
        #     application/json:
        #       schema:
        #         $ref: '#/components/schemas/TenantsCreateResponse'
        '201':
          description: 'Created<hr>
            The HTTP 201 Created success status response code indicates that the request has succeeded and has led to the creation of a resource.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsCreateResponse'                
        '204':
          description: 'Created, No Content<hr>
            There is no content to send for this request, but the headers may be useful.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    get:
      tags:
        - Tenants API
      description: Tenants `List`
      operationId: tenants-list
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        - $ref: '#/components/parameters/pOffset'
        - $ref: '#/components/parameters/pCount'
        - $ref: '#/components/parameters/pSort'
        - $ref: '#/components/parameters/pFilter'
        - $ref: '#/components/parameters/pFields'
        - $ref: '#/components/parameters/pETags'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsListResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/jh-identitymanagement/tenants/{TenantId}':
    get:
      tags:
        - Tenants API
      description: Tenants `Inquiry`
      operationId: tenants-inquire
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pTenantId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        - $ref: '#/components/parameters/pFields'
        # - $ref: '#/components/parameters/pRestrictions'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
            ETag:
              $ref: '#/components/headers/pETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsInquireResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'                
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    put:
      tags:
        - Tenants API
      description: Tenants `Replace`
      operationId: tenants-replace
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pTenantId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pIf-Match'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      requestBody:
        $ref: '#/components/requestBodies/TenantsReplaceRequest'
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsReplaceResponse'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '412':
          description: 'Precondition Failed<hr>
            Resources has been updated since last time it was retrieved.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    delete:
      tags:
        - Tenants API
      description: Tenants `Delete`
      operationId: tenants-delete
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pTenantId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pIf-Match'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsDeleteResponse'
        '204':
          description: 'No Content<hr>
            There is no content to send for this request, but the headers may be useful.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '412':
          description: 'Precondition Failed<hr>
            Resources has been updated since last time it was retrieved.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/jh-identitymanagement/tenants-search':
    post:
      tags:
        - Tenants API 
      description: Tenants `Search`
      operationId: tenants-search
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        - $ref: '#/components/parameters/pOffset'
        - $ref: '#/components/parameters/pCount'
        # - $ref: '#/components/parameters/pLanguage'
        - $ref: '#/components/parameters/pSort'
        - $ref: '#/components/parameters/pFields'
        # -- Custom Parameters --
      requestBody:
        $ref: '#/components/requestBodies/TenantsSearchRequest'
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsSearchResponse'
        # '201':
        #   description: 'Created<hr>
        #     The HTTP 201 Created success status response code indicates that the request has succeeded and has led to the creation of a resource.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        #   content:
        #     application/json:
        #       schema:
        #         $ref: '#/components/schemas/TenantsSearchResponse'         
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/jh-identitymanagement/tenants/{TenantId}/claims':
    post:
      tags:
        - Tenants Claims API
      description: Tenants Claims `Create`
      operationId: tenants-claims-create
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pTenantId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      requestBody:
        $ref: '#/components/requestBodies/TenantsClaimsCreateRequest'
      responses:
        # '200':
        #   description: 'OK<hr>
        #     The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        #   content:
        #     application/json:
        #       schema:
        #         $ref: '#/components/schemas/TenantsClaimsCreateResponse'
        '201':
          description: 'Created<hr>
            The HTTP 201 Created success status response code indicates that the request has succeeded and has led to the creation of a resource.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsClaimsCreateResponse'                
        '204':
          description: 'Created, No Content<hr>
            There is no content to send for this request, but the headers may be useful.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    get:
      tags:
        - Tenants Claims API
      description: Tenants Claims `List`
        <br><hr>Filtering - Elements supported by this API to filter results.
        <br>Filter Enumeration=["ClaimName","ClaimType","ClaimValue"]
      operationId: tenants-claims-list
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pTenantId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        - $ref: '#/components/parameters/pOffset'
        - $ref: '#/components/parameters/pCount'
        - $ref: '#/components/parameters/pSort'
        - $ref: '#/components/parameters/pFilter'
        - $ref: '#/components/parameters/pFields'
        - $ref: '#/components/parameters/pETags'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsClaimsListResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/jh-identitymanagement/tenants/{TenantId}/claims/{TenantClaimId}':
    get:
      tags:
        - Tenants Claims API
      description: Tenants Claims `Inquiry`
      operationId: tenants-claims-inquire
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pTenantId'
        - $ref: '#/components/parameters/pTenantClaimId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        - $ref: '#/components/parameters/pFields'
        # - $ref: '#/components/parameters/pRestrictions'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
            ETag:
              $ref: '#/components/headers/pETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsClaimsInquireResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'                
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    put:
      tags:
        - Tenants Claims API
      description: Tenants Claims `Replace`
      operationId: tenants-claims-replace
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pTenantId'
        - $ref: '#/components/parameters/pTenantClaimId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pIf-Match'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      requestBody:
        $ref: '#/components/requestBodies/TenantsClaimsReplaceRequest'
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsClaimsReplaceResponse'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '412':
          description: 'Precondition Failed<hr>
            Resources has been updated since last time it was retrieved.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    delete:
      tags:
        - Tenants Claims API
      description: Tenants Claims `Delete`
      operationId: tenants-claims-delete
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pTenantId'
        - $ref: '#/components/parameters/pTenantClaimId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pIf-Match'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsClaimsDeleteResponse'
        '204':
          description: 'No Content<hr>
            There is no content to send for this request, but the headers may be useful.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '412':
          description: 'Precondition Failed<hr>
            Resources has been updated since last time it was retrieved.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/jh-identitymanagement/tenants/{TenantId}/users/{UserUPN}/claims':
    post:
      tags:
        - Tenants Users Claims API
      description: Tenants Users Claims `Create`
      operationId: tenants-users-claims-create
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pTenantId'
        - $ref: '#/components/parameters/pUserUPN'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      requestBody:
        $ref: '#/components/requestBodies/TenantsUsersClaimsCreateRequest'
      responses:
        # '200':
        #   description: 'OK<hr>
        #     The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        #   content:
        #     application/json:
        #       schema:
        #         $ref: '#/components/schemas/TenantsUsersClaimsCreateResponse'
        '201':
          description: 'Created<hr>
            The HTTP 201 Created success status response code indicates that the request has succeeded and has led to the creation of a resource.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsUsersClaimsCreateResponse'                
        '204':
          description: 'Created, No Content<hr>
            There is no content to send for this request, but the headers may be useful.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    get:
      tags:
        - Tenants Users Claims API
      description: Tenants Users Claims `List`
      operationId: tenants-users-claims-list
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pTenantId'
        - $ref: '#/components/parameters/pUserUPN'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        # - $ref: '#/components/parameters/pLanguage'
        - $ref: '#/components/parameters/pIf-Modified-Since'
        - $ref: '#/components/parameters/pOffset'
        - $ref: '#/components/parameters/pCount'
        - $ref: '#/components/parameters/pSort'
        - $ref: '#/components/parameters/pFilter'
        - $ref: '#/components/parameters/pFields'
        - $ref: '#/components/parameters/pETags'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsUsersClaimsListResponse'
        '304':
          description: 'Not Modified <hr>
            The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
            It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
            or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/jh-identitymanagement/tenants/{TenantId}/users/{UserUPN}/claims/{TenantUserClaimId}':
    get:
      tags:
        - Tenants Users Claims API
      description: Tenants Users Claims `Inquiry`
      operationId: tenants-users-claims-inquire
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pTenantId'
        - $ref: '#/components/parameters/pUserUPN'
        - $ref: '#/components/parameters/pTenantUserClaimId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        - $ref: '#/components/parameters/pFields'
        # - $ref: '#/components/parameters/pRestrictions'
        # - $ref: '#/components/parameters/pLanguage'
        - $ref: '#/components/parameters/pIf-Modified-Since'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
            ETag:
              $ref: '#/components/headers/pETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsUsersClaimsInquireResponse'
        '304':
          description: 'Not Modified <hr>
            The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
            It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
            or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'                
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    put:
      tags:
        - Tenants Users Claims API
      description: Tenants Users Claims `Replace`
      operationId: tenants-users-claims-replace
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pTenantId'
        - $ref: '#/components/parameters/pUserUPN'
        - $ref: '#/components/parameters/pTenantUserClaimId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pIf-Match'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      requestBody:
        $ref: '#/components/requestBodies/TenantsUsersClaimsReplaceRequest'
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsUsersClaimsReplaceResponse'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '412':
          description: 'Precondition Failed<hr>
            Resources has been updated since last time it was retrieved.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    delete:
      tags:
        - Tenants Users Claims API
      description: Tenants Users Claims `Delete`
      operationId: tenants-users-claims-delete
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pTenantId'
        - $ref: '#/components/parameters/pUserUPN'
        - $ref: '#/components/parameters/pTenantUserClaimId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pIf-Match'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantsUsersClaimsDeleteResponse'
        '204':
          description: 'No Content<hr>
            There is no content to send for this request, but the headers may be useful.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '412':
          description: 'Precondition Failed<hr>
            Resources has been updated since last time it was retrieved.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/jh-identitymanagement/identitystores':
    post:
      tags:
        - IdentityStores API
      description: IdentityStores `Create`
      operationId: identityStores-create
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      requestBody:
        $ref: '#/components/requestBodies/IdentityStoresCreateRequest'
      responses:
        # '200':
        #   description: 'OK<hr>
        #     The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        #   content:
        #     application/json:
        #       schema:
        #         $ref: '#/components/schemas/IdentityStoresCreateResponse'
        '201':
          description: 'Created<hr>
            The HTTP 201 Created success status response code indicates that the request has succeeded and has led to the creation of a resource.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityStoresCreateResponse'
        '204':
          description: 'Created, No Content<hr>
            There is no content to send for this request, but the headers may be useful.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    get:
      tags:
        - IdentityStores API
      description: IdentityStores `List`
      operationId: identityStores-list
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        - $ref: '#/components/parameters/pOffset'
        - $ref: '#/components/parameters/pCount'
        - $ref: '#/components/parameters/pSort'
        - $ref: '#/components/parameters/pFilter'
        - $ref: '#/components/parameters/pFields'
        - $ref: '#/components/parameters/pETags'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityStoresListResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/jh-identitymanagement/identitystores/{IdentityStoreId}':
    get:
      tags:
        - IdentityStores API
      description: IdentityStores `Inquiry`
      operationId: identityStores-inquire
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pIdentityStoreId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        - $ref: '#/components/parameters/pFields'
        # - $ref: '#/components/parameters/pRestrictions'
        # - $ref: '#/components/parameters/pLanguage'
        # - $ref: '#/components/parameters/pIf-Modified-Since'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
            ETag:
              $ref: '#/components/headers/pETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityStoresInquireResponse'
        # '304':
        #   description: 'Not Modified <hr>
        #     The HTTP 304 Not Modified client redirection response code indicates that there is no need to retransmit the requested resources. 
        #     It is an implicit redirection to a cached resource. This happens when the request method is safe, like a GET or a HEAD request, 
        #     or when the request is conditional and uses a If-None-Match or a If-Modified-Since header.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'                
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    put:
      tags:
        - IdentityStores API
      description: IdentityStores `Replace`
      operationId: identityStores-replace
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pIdentityStoreId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pIf-Match'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      requestBody:
        $ref: '#/components/requestBodies/IdentityStoresReplaceRequest'
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityStoresReplaceResponse'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '412':
          description: 'Precondition Failed<hr>
            Resources has been updated since last time it was retrieved.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
    delete:
      tags:
        - IdentityStores API
      description: IdentityStores `Delete`
      operationId: identityStores-delete
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        - $ref: '#/components/parameters/pIdentityStoreId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pIf-Match'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        # - $ref: '#/components/parameters/pLanguage'
        # -- Custom Parameters --
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityStoresDeleteResponse'
        '204':
          description: 'No Content<hr>
            There is no content to send for this request, but the headers may be useful.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '412':
          description: 'Precondition Failed<hr>
            Resources has been updated since last time it was retrieved.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
  '/v2/jh-identitymanagement/identitystores-search':
    post:
      tags:
        - IdentityStores API 
      description: IdentityStores `Search`
      operationId: identityStores-search
      parameters:
        # - $ref: '#/components/parameters/pInstitutionId'
        # - $ref: '#/components/parameters/pInstUID'
        # - $ref: '#/components/parameters/pProductId'
        # - $ref: '#/components/parameters/pAuthorization'
        # - $ref: '#/components/parameters/pDate'
        # - $ref: '#/components/parameters/pContent-Type'
        - $ref: '#/components/parameters/pX-Request-ID'
        - $ref: '#/components/parameters/pX-Correlation-ID'
        - $ref: '#/components/parameters/pX-AuditUserId'
        - $ref: '#/components/parameters/pX-AuditDeviceId'
        - $ref: '#/components/parameters/pX-BusinessCorrelationId'
        - $ref: '#/components/parameters/pX-WorkflowCorrelationId'
        - $ref: '#/components/parameters/pX-AuthenticationUserCredential-optional'
        - $ref: '#/components/parameters/pX-AuthenticationProductCredential'
        - $ref: '#/components/parameters/pX-FaultOverride'
        - $ref: '#/components/parameters/pOffset'
        - $ref: '#/components/parameters/pCount'
        # - $ref: '#/components/parameters/pLanguage'
        - $ref: '#/components/parameters/pSort'
        - $ref: '#/components/parameters/pFields'
        # -- Custom Parameters --
      requestBody:
        $ref: '#/components/requestBodies/IdentityStoresSearchRequest'
      responses:
        '200':
          description: 'OK<hr>
            The HTTP 200 OK success status response code indicates that the request has succeeded. <br>A 200 response is cacheable by default.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityStoresSearchResponse'
        # '201':
        #   description: 'Created<hr>
        #     The HTTP 201 Created success status response code indicates that the request has succeeded and has led to the creation of a resource.'
        #   headers:
        #     X-Request-ID:
        #       $ref: '#/components/headers/pX-Request-ID'
        #     X-Correlation-ID:
        #       $ref: '#/components/headers/pX-Correlation-ID'
        #     X-Messages:
        #       $ref: '#/components/headers/pX-Messages'
        #     X-BusinessCorrelationId:
        #       $ref: '#/components/headers/pX-BusinessCorrelationId'
        #     X-WorkflowCorrelationId:
        #       $ref: '#/components/headers/pX-WorkflowCorrelationId'
        #   content:
        #     application/json:
        #       schema:
        #         $ref: '#/components/schemas/IdentityStoresSearchResponse'
        '400':
          description: 'Bad Request<hr>
            The server could not understand the request due to invalid syntax.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
            X-Messages:
              $ref: '#/components/headers/pX-Messages'
            X-BusinessCorrelationId:
              $ref: '#/components/headers/pX-BusinessCorrelationId'
            X-WorkflowCorrelationId:
              $ref: '#/components/headers/pX-WorkflowCorrelationId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageStatus'
        '401':
          description: 'Unauthorized<hr>
            Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". 
            That is, the client must authenticate itself to get the requested response.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '403':
          description: 'Forbidden<hr>
            The client does not have access rights to the content; that is, it is unauthorized, 
            so the server is refusing to give the requested resource. Unlike 401, the clients identity is known to the server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '404':
          description: 'Not Found<hr>
            The server can not find the requested resource. In an API, this can also mean that the endpoint is valid but 
            the resource itself does not exist. Servers may also send this response instead of 403 to hide the existence 
            of a resource from an unauthorized client.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '413':
          description: 'Request Too Large<hr>
            Request entity is larger than limits defined by server.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '422':
          description: 'Unprocessed<hr>
            The request was well-formed but was unable to be followed due to semantic errors.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        '500':
          description: 'Generic Internal Error<hr>
            The server has encountered a situation it doesnt know how to handle.'
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
        default:
          description: Generic Internal Error
          headers:
            X-Request-ID:
              $ref: '#/components/headers/pX-Request-ID'
            X-Correlation-ID:
              $ref: '#/components/headers/pX-Correlation-ID'
tags:
  - name: AdminConfigurations API
    description: AdminConfigurations OpenAPI Definitions
  - name: Users API
    description: Users OpenAPI Definitions
  - name: Users Claims API
    description: Users Claims OpenAPI Definitions
  - name: Users Groups API
    description: Users Groups OpenAPI Definitions
  - name: Groups API
    description: Groups OpenAPI Definitions
  - name: Tenants API
    description: Tenants OpenAPI Definitions
  - name: Tenants Claims API
    description: Tenants Claims OpenAPI Definitions
  - name: Tenants Users Claims API
    description: Tenants Users Claims OpenAPI Definitions
  - name: IdentityStores API
    description: IdentityStores OpenAPI Definitions
servers:
  - url: 'http:/jx-r.jhacorp.com/api'
    description: Corporate Test Server
  - url: 'http:/127.0.0.1/api'
    description: 'Local Mock Server #1'
  - url: 'http:/127.0.0.1:8080/api'
    description: 'Local Mock Server #2'
components:
  securitySchemes:
    BearerAuth:
      type: "http"
      description: "OpenIDConnect JWT Token"
      scheme: bearer
      bearerFormat: JWT
  parameters:
    pInstitutionUniversalId:
      name: InstitutionUniversalId
      in: path
      description: "The Institution Universal Identification given to an entity / institution.  The URL utilizes this Id for routing."
      required: true
      schema:
        type: string
        format: uuid
    pInstitutionId:
      name: InstitutionId
      in: path
      description: "The identification given to an entity / institution.  The URL utilizes this Id for routing.  A financial Institution 
        might opt to utilize their routing and transit number"
      required: true
      schema:
        type: string
        default: '000000000'
    pEnvironmentId:
      name: EnvironmentId
      in: path
      description: 'Institution Environment.  This code identifies the processing environment for which the message is intended.  
        Generally, the environment will be PRODuction, however, various other environments may exist.  TESTing environments are common.'
      required: true
      schema:
        type: string
        default: PROD
    pProductId:
      name: ProductId
      in: path
      description: 'Product Identifier.  This code identifies the Product for which the message is intended.  
        Generally, the Product Id will be in the form of Vendor-Product. However, various other Product Ids may exist. <br>Example:`jha-productname`'
      required: true
      schema:
        type: string
        example: jha-productname
    pResourceId:
      name: ResourceId
      in: path
      description: The identifier to be used as a key to the resource
      required: true
      schema:
        type: string
        example: 123e4567-e89b-12d3-a456-426655440000 
    pOffset:
      name: Offset
      in: query
      description: 'The service offset provided by the API provider
        <br>Example: `.../resource?Offset=value`'
      schema:
        type: string
        default: '0'
    pCount:
      name: Count
      in: query
      description: 'The count of records requested
        <br>Example: `.../resource?Count=value`'
      schema:
        type: string
        default: '0'
    pStatusType:
      name: StatusType
      in: path
      description: The type of status
      required: true
      schema:
        type: string
        default: ""
    pFields:
      name: Fields
      in: query
      description: 'The query of fields, comma separated, for get operations.
        <br>Example: `.../resource?Fields=field1,field2,field3`'
        # Optional Implementation` - API providers will remove references to this definition for APIs that will not implement the FIELDS behavior.
      schema:
        type: string
        example: "field1,field2,field3"
    pETags:
      name: ETags
      in: query
      description: 'Indicating if a collection of ETags should be returned for GET:List operations.  Default is false.
        <br>Example: `.../resource?ETags=true`'
        # Optional Implementation` - API providers will remove references to this definition for APIs that will not implement the ETags behavior.
      schema:
        type: boolean
        example: "false"
    pRestrictions:
      name: Restrictions
      in: query
      description: 'The query of restrictions
        <br>Example: `.../resource?Restrictions=value`'
      schema:
        type: boolean
        example: true
    pSort:
      name: Sort
      in: query
      description: 'Indicating the elements used for sorting and direction [asc, desc], comma separated, for operations that retun a list.
        <br>Example: `.../resource?Sort=field1:direction,field2:direction,field3:direction`'
      # Optional Implementation` - API providers will remove references to this definition for APIs that will not implement the SORT behavior.
      schema:
        type: string   
    pFilter-Basic:
      name: Filter
      in: query
      description: 'Indicating the elements used for filtering and the match value for each element.  Filtering reduces the rows returned and therefore 
        Basic Filtering is appropriate to services that return an array of resources.  The default operation is "equal" and the default collation rule is "ignore case"
        <br>Example: `.../resource?filter=address.state:mo,address.county:green)`'
        # Optional Implementation - API providers will remove references to this definition for APIs that will not implement the BASIC FILTER behavior.
        # Documentation of what fields/elements will be accepted for each API as filter items will be added to the API description.
      schema:
        type: string 
    pFilter:
      name: Filter
      in: query
      description: 'Indicating the elements used for filtering, the operation (type of evaluation), and the value used for evaluation for each element.  Filtering reduces the rows returned and therefore 
        Filtering is appropriate to services that return an array of resources.
        <br>Format of filter object is `element:operator:value`
        <br>Operators: eq - equal, ne - not equal, lt - less than, gt - greater than, in - in list, con - contains, gte - greater than or equal, lte - less than or equal, sw - starts with, ew - ends with
        <br>Collation Rule: i - ignore case (add rule to front of operation: ieq, ine, ilt, igt, iin, icon, igte, ilte, isw, iew)
        <br>Example: `.../resource?filter=address.state:eq:MO,address.county:eq:Green,customer.lastname:isw:hamm`'
        # Optional Implementation - API providers will remove references to this definition for APIs that will not implement the BASIC FILTER behavior.
        # Documentation of what fields/elements will be accepted for each API as filter items will be added to the API description.
      schema:
        type: string 
    pAuthorization:
      name: Authorization
      in: header
      description: "JWT Token. Represents Callers Authentication Token in the form of a JWT generated by a OAuth 2.0 Identity Provider 
        and conforms to the OpenID Connect specification.  See [Security section] for other details. 
        <br>Example `Authorization: bearer <GeneratedOidcJwt>`"
      required: true
      schema:
        type: string
        example: bearer <GeneratedOidcJwt>
    pDate: 
      in: header
      name: Date
      description: 'Standard http header element for date and time. HTTP headers are represented as RFC 7231 Full Dates. 
        <br>Example `Date: Wed, 21 Oct 2015 07:28:00 GMT`'
      required: true
      schema:
        type: string
        example: Wed, 21 Oct 2015 07:28:00 GMT
    pContent-Type:
      name: Content-Type
      description: 'The Content-Type entity header is used to indicate the media type of the resource. In responses, a Content-Type header 
        tells the client what the content type of the returned content actually is. In requests, (such as POST or PUT), the client tells the 
        server what type of data is actually sent.'
      in: header
      required: true
      schema:
        type: string
        default: application/json
    pETag:
      name: ETag
      in: header
      description: 'Entity tag used for web cache validation.  It provides a mechanism to cache unchanged resources.'
      schema:
        type: string
    pIf-Match:
      name: If-Match
      in: header
      description: 'Enables optimistic updating with ETag'
      schema:
        type: string
    pIf-Modified-Since:
      name: If-Modified-Since
      in: header
      description: 'The If-Modified-Since request HTTP header makes the request conditional: the server will send back the requested resource, 
        with a 200 status, only if it has been last modified after the given date. If the request has not been modified since, the response will 
        be a 304 without any body; the Last-Modified response header of a previous request will contain the date of last modification. 
        <br>Example: `<day-name>, <day> <month> <year> <hour>:<minute>:<second> GMT`'
      schema:
        type: string
        example: "<day-name>, <day> <month> <year> <hour>:<minute>:<second> GMT"
    pX-Request-ID:
      name: X-Request-ID
      in: header
      description: 'To support communications level correlation due to some responses only having header data, a round-trip identifier (GUID) 
        will be sent on every call from the consumer and returned by the service provider (or intermediary) regardless of the final disposition of the request/message.'
      required: true
      schema:
        type: string
        format: uuid
        example: 123e4567-e89b-12d3-a456-426655440000
    pX-AuditUserId:
      name: X-AuditUserId
      in: header
      description: 'This is the User Id which the consumer would like written to the audit as performing the requested service. It will vary but 
        could be down to the user id. It will not be use to authenticate, only audit information.'
      schema:
        type: string
        example: InigoMontoya
    pX-AuditDeviceId:
      name: X-AuditDeviceId
      in: header
      description: This is the device Id which the consumer would like written to the audit as performing the requested service. It will vary but could be down to the machine id or address.
      schema:
        type: string
        example: 192.168.0.101
    pX-BusinessCorrelationId:
      name: X-BusinessCorrelationId
      in: header
      description: The correlation identification as related to business functions and  activities.  Generally expected to be a GUID.
      schema:
        type: string
        example: 123e4567-e89b-12d3-a456-426655440000
    pX-WorkflowCorrelationId:
      name: X-WorkflowCorrelationId
      in: header
      description: The correlation identification as related to workflow functions and activities. Generally expected to be a GUID.
      schema:
        type: string
        example: 123e4567-e89b-12d3-a456-426655440000
    pX-AuthenticationUserCredential-optional:
      name: X-AuthenticationUserCredential
      in: header
      description: 'Authentication of the end-user (person at keyboard) Credentials in the form of a JSON Web Token (JWT). <br>Format is `<type> <token>`. Type default is jwt.
        <br>Example `X-AuthenticationUserCredential: jwt <GeneratedOidcJwt>` 
        <br>Example `X-AuthenticationUserCredential: saml <Generatedsaml>`'
      schema:
        type: string
        example: jwt:<GeneratedOidcJwt>
    pX-AuthenticationProductCredential:
      name: X-AuthenticationProductCredential
      in: header
      description: 'Authentication of the Consumer Product Credentials in the form of a JSON Web Token (JWT).  Same as "Authorization" 
        jwt unless the message is sent "on-behalf-of" this consumer product. <br>Format is `<type> <token>`. Type default is jwt.
        <br>Example `X-AuthenticationProductCredential: jwt <GeneratedOidcJwt>` 
        <br>Example `X-AuthenticationProductCredential: saml <Generatedsaml>`'
      required: true
      schema:
        type: string
        example: jwt:<GeneratedOidcJwt>
    pX-Correlation-ID:
      name: X-Correlation-ID
      in: header
      required: true
      description: Correlation ID for communication-based multi-hop message correlation
      schema:
        type: string
    pX-Messages:
      name: X-Messages
      in: header
      description: Indicating messages/faults/errors returned
      schema:
        type: boolean
        default: false
    pX-Version:
      name: X-Version
      in: header
      description: Indicating version for the management status service
      schema:
        type: string
        default: false
    pX-FaultOverride:
      name: X-FaultOverride
      in: header
      description: Determines if all faults should be override
      schema:
        type: string
        default: false
    pContentRange:
      name: Content-Range
      in: header
      description: "Http header element that indicates where, in a full body message, a partial message belongs.
                <br> Examples:
                <br>`Content-Range: <unit> <range-start>-<range-end>/<size>`
                <br>`Content-Range: <unit> <range-start>-<range-end>/*`
                <br>`Content-Range: <unit> */<size>`"
      required: true
      example: "bytes 0-1000/*"
      schema:
        type: string
        example: bytes 0-1000/*
    pLanguage:
      name: Language
      in: query
      description: 'Language of request
        <br>Example: `.../resource?Language=value`'
      schema:
        type: string
        default: en
        enum: [en, sp, fr, ch]
    #- - - Custom Parameters - - - 
    pAdminConfigurationId:
      name: AdminConfigurationId
      in: path
      description: 'Custom URL key: AdminConfigurationId'
      required: true
      schema:
        type: string
    pUserUPN:
      name: UserUPN
      in: path
      description: 'Custom URL key: UserUPN'
      required: true
      schema:
        type: string
    pClaimsId:
      name: ClaimsId
      in: path
      description: 'Custom URL key: ClaimsId'
      required: true
      schema:
        type: string
    pGroupDistinguishedName:
      name: GroupDistinguishedName
      in: path
      description: 'Custom URL key: GroupDistinguishedName'
      required: true
      schema:
        type: string
    pTenantId:
      name: TenantId
      in: path
      description: 'Custom URL key: TenantId'
      required: true
      schema:
        type: string
    pTenantClaimId:
      name: TenantClaimId
      in: path
      description: 'Custom URL key: TenantClaimId'
      required: true
      schema:
        type: string
    pTenantUserClaimId:
      name: TenantUserClaimId
      in: path
      description: 'Custom URL key: TenantUserClaimId'
      required: true
      schema:
        type: string
    pIdentityStoreId:
      name: IdentityStoreId
      in: path
      description: 'Custom URL key: IdentityStoreId'
      required: true
      schema:
        type: string    
  headers:
    pETag:
      description: 'Entity tag used for web cache validation.  It provides a mechanism to cache unchanged resources.'
      schema:
        type: string
        default: application/json
    pIf-Match:
      description: 'Enables optimistic updating with ETag'
      schema:
        type: string
        default: application/json
    pX-Request-ID:
      description: 'To support communications level correlation due to some responses only having header data, a round-trip identifier 
        (GUID) will be sent on every call from the consumer and returned by the service provider (or intermediary) regardless of the final 
        disposition of the request/message.'
      required: true
      schema:
        type: string
        format: uuid
        example: 123e4567-e89b-12d3-a456-426655440000
    pX-AuditUserId:
      description: 'This is the User Id which the consumer would like written to the audit as performing the requested service. It will 
        vary but could be down to the user id. It will not be use to authenticate, only audit information.'
      schema:
        type: string
    pX-AuditDeviceId:
      description: 'This is the device Id which the consumer would like written to the audit as performing the requested service. It will 
        vary but could be down to the machine id.'
      schema:
        type: string
    pX-BusinessCorrelationId:
      description: The correlation identification as related to business functions and  activities.  Generally expected to be a GUID.
      schema:
        type: string
    pX-WorkflowCorrelationId:
      description: The correlation identification as related to workflow functions and activities. Generally expected to be a GUID.
      schema:
        type: string
    pX-AuthenticationUserCredential-optional:
      description: 'Authentication of the end-user (person at keyboard) Credentials in the form of a JSON Web Token (JWT). 
        <br>Example `X-AuthenticationUserCredential: jwt:<GeneratedOidcJwt>` 
        <br>Example `X-AuthenticationUserCredential: saml:<Generatedsaml>`'
      schema:
        type: string
    pX-AuthenticationProductCredential:
      description: 'Authentication of the Consumer Product Credentials in the form of a JSON Web Token (JWT).  Same as "Authorization" 
        jwt unless the message is sent "on-behalf-of" this consumer product.  
        <br>Example `X-AuthenticationProductCredential: jwt:<GeneratedOidcJwt>` 
        <br>Example `X-AuthenticationProductCredential: saml:<Generatedsaml>`'
      required: true
      schema:
        type: string
    pX-Correlation-ID:
      description: Correlation ID for communication-based multi-hop message correlation
      schema:
        type: string
    pX-Messages:
      description: Indicating messages/faults/errors returned
      schema:
        type: boolean
        default: false
    pX-Version:
      description: Indicating version for the management status service
      schema:
        type: string
        default: false
    pX-FaultOverride:
      description: Determines if all faults should be override
      schema:
        type: string
        default: false
  requestBodies:
    AdminConfigurationsCreateRequest:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AdminConfigurationsCreateRequest'
    AdminConfigurationsReplaceRequest:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AdminConfigurationsReplaceRequest'
    UsersSearchRequest:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/UsersSearchRequest'
    TenantsCreateRequest:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/TenantsCreateRequest'
    TenantsReplaceRequest:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/TenantsReplaceRequest'
    TenantsSearchRequest:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/TenantsSearchRequest'
    TenantsClaimsCreateRequest:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/TenantsClaimsCreateRequest'
    TenantsClaimsReplaceRequest:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/TenantsClaimsReplaceRequest'
    TenantsUsersClaimsCreateRequest:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/TenantsUsersClaimsCreateRequest'
    TenantsUsersClaimsReplaceRequest:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/TenantsUsersClaimsReplaceRequest'
    IdentityStoresCreateRequest:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/IdentityStoresCreateRequest'
    IdentityStoresReplaceRequest:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/IdentityStoresReplaceRequest'
    IdentityStoresSearchRequest:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/IdentityStoresSearchRequest'
  schemas:
    AdminConfigurationsCreateRequest:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/AdminConfigurationsCreateRequest'
    AdminConfigurationsCreateResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/AdminConfigurationsCreateResponse'
    AdminConfigurationsListResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/AdminConfigurationsListResponse'
    AdminConfigurationsInquireResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/AdminConfigurationsInquireResponse'
    AdminConfigurationsReplaceRequest:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/AdminConfigurationsReplaceRequest'
    AdminConfigurationsReplaceResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/AdminConfigurationsReplaceResponse'
    AdminConfigurationsDeleteResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/AdminConfigurationsDeleteResponse'
    UsersListResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/UsersListResponse'
    UsersInquireResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/UsersInquireResponse'
    UsersSearchRequest:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/UsersSearchRequest'
    UsersSearchResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/UsersSearchResponse'
    UsersClaimsListResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/UsersClaimsListResponse'
    UsersGroupsListResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/UsersGroupsListResponse'
    UsersGroupsInquireResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/UsersGroupsInquireResponse'
    GroupsListResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/GroupsListResponse'
    GroupsInquireResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/GroupsInquireResponse'
    TenantsCreateRequest:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsCreateRequest'
    TenantsCreateResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsCreateResponse'
    TenantsListResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsListResponse'
    TenantsInquireResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsInquireResponse'
    TenantsReplaceRequest:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsReplaceRequest'
    TenantsReplaceResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsReplaceResponse'
    TenantsDeleteResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsDeleteResponse'
    TenantsSearchRequest:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsSearchRequest'
    TenantsSearchResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsSearchResponse'
    TenantsClaimsCreateRequest:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsClaimsCreateRequest'
    TenantsClaimsCreateResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsClaimsCreateResponse'
    TenantsClaimsListResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsClaimsListResponse'
    TenantsClaimsInquireResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsClaimsInquireResponse'
    TenantsClaimsReplaceRequest:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsClaimsReplaceRequest'
    TenantsClaimsReplaceResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsClaimsReplaceResponse'
    TenantsClaimsDeleteResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsClaimsDeleteResponse'
    TenantsUsersClaimsCreateRequest:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsUsersClaimsCreateRequest'
    TenantsUsersClaimsCreateResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsUsersClaimsCreateResponse'
    TenantsUsersClaimsListResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsUsersClaimsListResponse'
    TenantsUsersClaimsInquireResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsUsersClaimsInquireResponse'
    TenantsUsersClaimsReplaceRequest:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsUsersClaimsReplaceRequest'
    TenantsUsersClaimsReplaceResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsUsersClaimsReplaceResponse'
    TenantsUsersClaimsDeleteResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/TenantsUsersClaimsDeleteResponse'  
    IdentityStoresCreateRequest:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/IdentityStoresCreateRequest'
    IdentityStoresCreateResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/IdentityStoresCreateResponse'
    IdentityStoresListResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/IdentityStoresListResponse'
    IdentityStoresInquireResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/IdentityStoresInquireResponse'
    IdentityStoresReplaceRequest:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/IdentityStoresReplaceRequest'
    IdentityStoresReplaceResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/IdentityStoresReplaceResponse'
    IdentityStoresDeleteResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/IdentityStoresDeleteResponse'
    IdentityStoresSearchRequest:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/IdentityStoresSearchRequest'
    IdentityStoresSearchResponse:
      $ref: './schemas/jh-ims-ent-api.v1.json#/properties/IdentityStoresSearchResponse'
    MessageStatus:
      $ref: '../common/schemas/ComplexDictionary.v1.json#/definitions/MessageStatus'
